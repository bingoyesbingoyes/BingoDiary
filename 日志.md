### 24.12.28

- Add Export -> JSON, PDF
- Add Load <- JSON
- Add Info: show storage path, change storage path

### 25.03.27

- Support image now.

### 25.03.28

- Support load / export image.
- Change background and adjust bg color.

打包时遇到网络问题，于是：yarn config set registry https://registry.npm.taobao.org

重装依赖时，qcalender出现问题，在删除后（quasar ext remove @quasar/qcalendar ），神奇解决

### 25.12.27
From Quasar + Electron -> Tauri + Rust
有了项目级AI的加持，能实现很多曾经做不到的事。

### 25.12.29
添加了settings，更高的定制性。
打包时因为qcalender依赖，需要yarn add @popperjs/core@^2.0.0
同时src-tauri/Cargo.toml中需要替换katex = { version = "0.4", default-features = false, features = ["duktape"] }

### 25.12.30
添加了zen mod，快捷键，tag，还有Apple Style的视觉。
设计了new logo

### 25.12.31
支持移动端
对于build时签名问题
keytool -genkey -v -keystore D:\bingoyes\BingoDiary\src-tauri\gen\android\app\bingo-diary.jks -keyalg RSA -keysize 2048 -validity 10000 -alias bingo-diary
注意修改keystore.properties中pwd和Alias

对于初始化Tauri Android
npx @tauri-apps/cli android init
